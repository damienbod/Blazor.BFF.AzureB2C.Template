name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0'
        include-prerelease: True
    - name: Restore dependencies
      run: dotnet restore ./content/BlazorBffAzureB2C/Server
    - name: Build STS
      run: dotnet build ./content/BlazorBffAzureB2C/Server/Blazor.Bff.AzureB2c.Server.csproj --no-restore
